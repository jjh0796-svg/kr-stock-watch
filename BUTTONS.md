# 공시 메시지 버튼

새 공시/보완 요약의 마지막 메시지에 버튼을 붙인다. IPO·다른 분야 봇에는 아직 적용하지 않는다.

- 이 사건 추적 / 추적 중·끄기: DART 문서 family로 연결되는 추가 후속 확인을 켜고 끈다. 원래 구독 중인 공시는 필터링하지 않는다. 날짜가 없는 공시도 회사 식별정보가 있으면 수동 추적 가능.
- 일정 알림 끄기 / 켜기: 해당 사건의 예정일 및 결과 미확인 안내만 조절한다. 정정·철회·결과 공시 확인은 유지한다. 이미 보낸 안내는 켜도 반복하지 않는다.
- 유용함 / 불필요함: 공시별 마지막 평가를 저장한다. 반대 평가를 누르면 수정한다. 평가만으로 공시를 차단하거나 AI 필터를 자동 변경하지 않는다.

기존 process_commands에서 callback_query를 처리한다. 별도 getUpdates 소비자는 만들지 않는다. GitHub 작업 실행 공백이나 긴 원문 조회 중에는 버튼 응답이 지연될 수 있다.
허용된 1:1 채팅, 동일 사용자 ID, 실제 발송된 메시지 ID, 등록된 버튼 참조를 모두 검증한다. 다른 사용자/채팅/위조 참조는 설정을 변경하지 않는다.
선택은 dart_seen.json에 먼저 원자적으로 저장한 뒤 Telegram 확인 표시와 버튼 갱신을 요청한다. 저장 실패 시 update offset을 전진시키지 않는다. 켜기/끄기는 명시적 설정이므로 같은 콜백 재처리가 반전시키지 않는다.
상태는 기존 .state 캐시에 이어서 보관된다. 캐시 자체가 유실되면 이전 버튼을 임의 복원하지 않고 기록 없음을 안내한다.

검증: `python -m pytest tests/test_message_buttons.py tests/test_disclosures.py tests/test_followups.py`
